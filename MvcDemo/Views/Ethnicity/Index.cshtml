@model IEnumerable<MvcDemo.Models.ETHINICITY_TABLE>

@{
    ViewBag.Title = "Ethinicities";
}

<h2>Ethinicities</h2>
@if (Roles.IsUserInRole("admin"))
{
}
else
{
    //redirects to page "AdminError"
    Response.Redirect("~/Home/AdminError");
}
<h3 style="color: red;">@TempData["notice"]</h3>

<p>
    @Html.ActionLink("Back", "adminOnly", "Home") |
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ETHINICITY_ID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ETHINICITY)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ETHINICITY_ID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ETHINICITY)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new {  id=item.ETHINICITY_ID  }) |
            @Html.ActionLink("Details", "Details", new {  id=item.ETHINICITY_ID  }) |
            @Html.ActionLink("Delete", "Delete", new {  id=item.ETHINICITY_ID  })
        </td>
    </tr>
}

</table>
