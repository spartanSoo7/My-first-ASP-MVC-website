@model MvcDemo.Models.CAMPUS_TABLE

@{
    ViewBag.Title = "Create";
}

@if (Roles.IsUserInRole("admin"))
{
}
else
{
    //redirects to page "AdminError"
    Response.Redirect("~/Home/AdminError");
}


<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>CAMPUS_TABLE</legend>

        <table>
            <tr>
                <th>@Html.HiddenFor(model => model.CAMPUS_ID)</th>
                <td>
                    @Html.HiddenFor(model => model.CAMPUS_ID)
                    @Html.ValidationMessageFor(model => model.CAMPUS_ID)
                </td>
            </tr>
            <tr>
                <th>@Html.LabelFor(model => model.CAMPUS_NAME)</th>
                <td>
                    @Html.EditorFor(model => model.CAMPUS_NAME)
                    @Html.ValidationMessageFor(model => model.CAMPUS_NAME)
                </td>
            </tr>
        </table>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
