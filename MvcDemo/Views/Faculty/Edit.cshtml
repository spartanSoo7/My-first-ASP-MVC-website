@model MvcDemo.Models.FACULTY_TABLE

@{
    ViewBag.Title = "Edit";
}
@if (Roles.IsUserInRole("admin"))
{
}
else
{
    //redirects to page "AdminError"
    Response.Redirect("~/Home/AdminError");
}


<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>FACULTY_TABLE</legend>

        <div class="editor-label">
            @Html.HiddenFor(model => model.FACULTY_ID)
        </div>
        <div class="editor-field">
            @Html.HiddenFor(model => model.FACULTY_ID)
            @Html.ValidationMessageFor(model => model.FACULTY_ID)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.FACULTY_NAME)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.FACULTY_NAME)
            @Html.ValidationMessageFor(model => model.FACULTY_NAME)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
